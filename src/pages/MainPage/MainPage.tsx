import React, {FC} from 'react';
import styles from './MainPage.module.scss';

const MainPage: FC = () => {
    return (
        <div>
            <h1>Main</h1>
        </div>
    );
};

export default MainPage;
